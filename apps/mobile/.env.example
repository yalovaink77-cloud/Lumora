# Absolute API origin only. No path, query, fragment, or credentials.
# Android emulator (API on host): http://10.0.2.2:3000
# iOS simulator: http://localhost:3000
# Physical device on LAN: http://192.168.x.x:3000
# Production/release builds: https://api.example.com
EXPO_PUBLIC_LUMORA_API_BASE_URL=http://localhost:3000

# Set true for release builds that must reject HTTP.
# EXPO_PUBLIC_LUMORA_REQUIRE_HTTPS=true
